interface MyComponentProps {
    children?: React.ReactNode;
}

type statePoint = {
    x: number;
    y: number;
    color: string;
};

type userTypes = {
    avatar: string | StaticImport;
    email: string;
    uid: string;
    username: string;
};
